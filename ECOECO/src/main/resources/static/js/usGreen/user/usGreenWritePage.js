    // 클릭 횟수를 저장할 변수들
    let likeCounts = Array.from({ length: 10 }, () => 0);
    let dislikeCounts = Array.from({ length: 10 }, () => 0);

    // 사용자가 좋아요나 아쉬워요를 클릭했는지 여부를 추적하는 배열
    let likedByUser = Array.from({ length: 10}, () => false);
    let dislikedByUser = Array.from({ length: 10}, () => false);

    // 각 박스에 대한 클릭 이벤트 처리
    for (let i = 1; i <= 10; i++) {
        let likeButton = document.getElementById('clickBox1');
        let dislikeButton = document.getElementById('clickBox2');
    }


    // 좋아요 버튼에 대한 클릭 이벤트 처리


    // 아쉬워요 버튼에 대한 클릭 이벤트 처리

